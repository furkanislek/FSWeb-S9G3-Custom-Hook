import { useState } from "react";


function LastestLocalStorage({themCh}) {
    function themeSelect(themCh){
        
        

        if(theme === true){
            themCh("dark-mode App");
        }else[
            themCh("App")
        ];
    
    }
  return themeSelect;
}

export default LastestLocalStorage;